<!DOCTYPE html>
<html>
  <head>
    <title>RailsAuthentication</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%# If you put the partials in the views/application folder, you can just render them without the "application/" part of the path %>
    <%= render "navbar" %>
    <%= render "flashes" %>
    <%= yield %>
  </body>
</html>
